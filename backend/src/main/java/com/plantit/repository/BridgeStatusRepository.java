package com.plantit.repository;

import com.plantit.entity.BridgeStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BridgeStatusRepository extends JpaRepository<BridgeStatus, Long> {
}
